#include "utils.h"
#include "constants.h"
#include "math.h"
#include "structures.h"

//int checkScaleCoeffs(const scaleT &s) {
//    if (fabs(scale.kx) < EPS || fabs(scalel.ky) < EPS || fabs(scale.kz) < EPS) {
//        return ERR_SCALE_COEFF;
//    }

//    return OK;
//}
